# Production — set in Cloudflare Pages → Settings → Environment variables
# Use your Oracle VM domain (nginx) or direct wss endpoint:
# VITE_SERVER_URL=wss://game.yourdomain.com/ws
#
# If exposing port 3001 directly (not recommended without TLS):
# VITE_SERVER_URL=wss://YOUR_ORACLE_IP:3001/ws
